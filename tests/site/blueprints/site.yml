
sections:
    pages:
        type: pages

    fields:
        type: fields
        fields:
            title:
                type: text
            nodatajob:
                type: janitor
                job: 'minimal'
                # no data
            jansite:
                type: janitor
                job: 'clipboard'
                data: '{{ model }} {{ site.title }}'
