
sections:
    pages:
        type: pages

    fields:
        type: fields
        fields:
            title:
                type: text
            nodatajob:
                type: janitor
                command: 'minimal'
                # no data
            jansite:
                type: janitor
                command: 'clipboard'
                data: '{{ model }} {{ site.title }}'
