sections:
  pages:
    type: pages

  fields:
    type: fields
    fields:
      title:
        type: text
      nodatajob:
        type: janitor
        command: whistle
        # no data
      jansite:
        type: janitor
        command: 'janitor:clipboard --data {{ site.title }}'
